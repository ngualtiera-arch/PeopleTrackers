{
  "name": "@peopletrackers/db",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/client.ts",
  "prisma": {
    "seed": "tsx --env-file=.env prisma/seed.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "migrate": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "tsx --env-file=.env prisma/seed.ts",
    "studio": "prisma studio"
  },
  "dependencies": {
    "@peopletrackers/shared": "*",
    "@prisma/client": "^5.20.0",
    "argon2": "^0.41.1"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
